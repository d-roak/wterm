{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  },
  "npm": false,
  "hooks": {
    "after:bump": "npm pkg set version=${version} --prefix packages/wterm"
  }
}
